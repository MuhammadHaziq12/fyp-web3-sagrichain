import React from 'react'
import AddProducts from "@/src/components/dashboard/common/AddProducts.jsx"

export const metadata = {
  title: "Farmer Dashboard: Add Product",
};

const page = () => {
  return (
    <div>
        <AddProducts/>
    </div>
  )
}

export default page
